service: brevis
tenant: orthodoz
app: brevis-app
org: orthodoz

provider:
  name: aws
  runtime: nodejs12.x
  stage: prod
  region: us-east-1

functions:
  hello:
    handler: handle.hello
    events:
      - http:
          path: first-api
          method: GET
      - http:
          path: post-api
          method: POST
